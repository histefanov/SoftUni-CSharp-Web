<div class="alert alert-danger alert-dismissable">
    <a href="javascript:window.history.back()" class="close" aria-label="close">×</a>
    <strong>Your input is not valid:</strong>
        <ul>
            @foreach (var error in Model)
            {
                <li>
                    <h4>@error</h4>
                </li>
            }
        </ul>
</div>